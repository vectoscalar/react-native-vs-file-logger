import { palette } from './palette'

const colors = {
  dark: palette.dark,
  light: palette.white,
  primary: palette.blue,
  success: palette.green,
  danger: palette.red,
}

export const AppColors = colors
